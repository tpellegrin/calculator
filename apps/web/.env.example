# Base URL of the calculator API (Go service).
# Leave empty to make same-origin requests (e.g., behind a Vite dev proxy).
# Must include a scheme (http:// or https://) if set.
VITE_API_BASE_URL=http://localhost:8080
