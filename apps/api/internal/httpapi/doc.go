// Package httpapi owns HTTP request decoding, response encoding, status mapping,
// and handler composition. It translates between the transport contract and the
// calculator domain.
package httpapi
